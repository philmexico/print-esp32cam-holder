$fn = 50;

bar_radius = 8;
bar_height = 500;

clamp_height = 20;
clamp_thickness = 3;

clamp_lock_width = 2 * clamp_thickness;
clamp_lock_thickness = .75 * clamp_thickness;

hinge_radius = clamp_thickness;
hinge_axis_radius = clamp_thickness / 2;
hinge_height = clamp_height / 2;

hinge_position_x = bar_radius + clamp_thickness + hinge_axis_radius;

printer_tolerance = .2;

screwhole_radius = 1;